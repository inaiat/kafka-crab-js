
[package]
authors = ["Inaiat <inaiatgmail.com>"]
edition = "2021"
name = "pdf_crab_js"
version = "0.3.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = "3.12.0"
napi-derive = "3.6.1"
pdf-writer = "0.15.0"

[build-dependencies]
napi-build = { version = "2.4.0" }

[profile.release]
lto = true
strip = "symbols"
